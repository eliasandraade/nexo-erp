# Production environment — baked into the Vite bundle at build time.
# Railway frontend service should also set this as an env var
# so it can be overridden per-environment without a redeploy.
VITE_API_BASE_URL=https://backend-production-b2bc.up.railway.app/api
